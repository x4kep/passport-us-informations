import Button from "@mui/material/Button";

const TermsConditions = () => {
  return (
    <h1>
      TermsConditions <Button variant="contained">Contained</Button>
    </h1>
  );
};

export default TermsConditions;
